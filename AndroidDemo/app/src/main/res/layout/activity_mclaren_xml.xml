<?xml version="1.0" encoding="utf-8"?>
<!--
    Uses RecyclerView with StaggeredGridLayoutManager
    Demonstrates traditional Android UI development
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/appBarLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1A1A1A">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#1A1A1A"
            app:subtitle="XML • RecyclerView"
            app:subtitleTextColor="#FF6B00"
            app:title="McLaren Gallery"
            app:titleTextColor="@android:color/white" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- RecyclerView 使用 layout_behavior 自动排列在 AppBarLayout 下方 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewMcLaren"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:padding="8dp"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:listitem="@layout/item_mclaren_car" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
